# 💰 Настройка Google AdSense для AutoNews (Next.js)

## 🎯 Стратегия: "Гибридный подход" (Auto Ads + Manual)
Google AdSense сильно эволюционировал, и сейчас лучшая практика заработка для новостных сайтов — это **Гибридный подход**.

1. **Google Auto Ads (ИИ Гугла)** — анализирует страницу и сам вставляет рекламу туда, где она не портит дизайн, но приносит больше всего кликов. Это включает полноэкранные заставки (Vignette) и плавающие баннеры (Anchor).
2. **Manual Ad Units (Жёсткая вставка)** — React-компоненты, которые мы можем жестко закрепить в сайдбаре (Sticky Sidebar) или под главным изображением статьи. Это гарантирует 100% просмотры у каждого пользователя.

---

## 📋 Текущий статус: ПРОЙДЕНА ПРОВЕРКА

### ✅ 1. Базовый скрипт уже работает!
Скрипт AdSense успешно интегрирован в корень фронтенда через React-компонент, загружаясь асинхронно и не блокируя рендеринг:
- **Файл:** `frontend-next/components/public/AdSenseScript.tsx`
- **Ваш Publisher ID:** `ca-pub-1075473812019633`

*Если вы уже включили Auto Ads в панели управления Google AdSense, реклама начнет появляться на сайте автоматически после одобрения домена!*

### ✅ 2. Юридические страницы (Google Publisher Policies)
Для прохождения модерации в AdSense на сайте обязательны следующие страницы. **Они уже созданы и доступны:**
- `/about` — Страница "О нас"
- `/contact` — Контакты
- `/privacy-policy` — Политика конфиденциальности
- `/terms` — Условия использования

---

## 🛠 Как добавить "Жёсткие вставки" (React Manual Ads)

Если вы решите, что автоматических показов от Гугла недостаточно, мы можем создать кастомный `Next.js` компонент для рекламы.

### 1. Создание блока в AdSense
1. В панели AdSense: **Реклама** → **По блоку объявлений** → **Дисплейная реклама** (Display Ads).
2. Выберите тип (Квадрат или Горизонтальный) и размер (Responsive).
3. Нажмите "Создать" и скопируйте `data-ad-slot` (цифры).

### 2. Вставка на фронтенд (в будущем)
Если вам это понадобится, разработчик создаст компонент `AdBanner` в `frontend-next/components/public/AdBanner.tsx` со следующим кодом:

```tsx
'use client';
import { useEffect } from 'react';

interface AdBannerProps {
  dataAdSlot: string;
  dataAdFormat?: string;
  dataFullWidthResponsive?: boolean;
}

export default function AdBanner({ dataAdSlot, dataAdFormat = 'auto', dataFullWidthResponsive = true }: AdBannerProps) {
  useEffect(() => {
    try {
      // @ts-ignore
      (window.adsbygoogle = window.adsbygoogle || []).push({});
    } catch (err) {
      console.error('AdSense Error', err);
    }
  }, []);

  return (
    <ins className="adsbygoogle"
         style={{ display: 'block' }}
         data-ad-client="ca-pub-1075473812019633"
         data-ad-slot={dataAdSlot}
         data-ad-format={dataAdFormat}
         data-full-width-responsive={dataFullWidthResponsive.toString()}></ins>
  );
}
```

Этот компонент можно будет вызывать в любом месте (например, на странице статьи `/articles/[slug]` или в сайдбаре).

---

## 📈 Следующие шаги:
1. Зайдите в аккаунт **Google AdSense**.
2. Убедитесь, что `freshmotors.net` добавлен во вкладку "Сайты" (Sites).
3. Зайдите во вкладку "Реклама" (Ads) -> "Автоматизированная реклама" (Auto Ads) и включите ее.
4. Дождитесь модерации (от 2 дней до 2 недель). Гугл проверит качество текстов, уникальность статeй и наличие юридических страниц.
