# 🔒 Улучшения безопасности (20 января 2026)

## ✅ Что было добавлено

### 1. **Rate Limiting - Защита от спама**
- Генерация статей: **макс 5 запросов/минуту** на IP
- Рейтинги: **макс 10 запросов/час** на IP
- Автоматическая блокировка при превышении (HTTP 429)

### 2. **Валидация загружаемых файлов**
- Максимальный размер: **10 MB**
- Разрешенные форматы: JPG, JPEG, PNG, WEBP
- Проверка MIME-типа (защита от подделки)

### 3. **Улучшенная защита рейтингов**
- Fingerprinting: **IP + User Agent**
- MD5 хеш для уникальности
- Сложнее обходить через VPN

### 4. **Обновлен .env.example**
- Убраны старые ссылки на Gemini (используем только Groq)
- Добавлены комментарии по безопасности
- Инструкции по генерации SECRET_KEY

---

## 📝 Ответы на вопросы

### **Про API ключ в переписке:**
- В будущем: маскируй ключи `GROQ_API_KEY=gsk_***hidden***`
- Не показывай содержимое `.env` файла
- Используй `.env.example` для документации

### **Что могут сделать с Groq ключом:**
1. Исчерпать твою квоту (30 запросов/минуту)
2. Потратить деньги (если платный план)
3. Заблокировать аккаунт (за нарушения)
4. Увидеть что ты генерируешь

**Рекомендация:** Смени ключ на https://console.groq.com/keys

### **DEBUG=True - что это:**
Django показывает подробные ошибки:
- Полные пути к файлам
- Код вокруг ошибки
- Переменные окружения
- Структуру БД

**В production это опасно** - дает хакерам карту сервера!

### **CORS_ALLOW_ALL_ORIGINS:**
Без этого: только `localhost:3000` → `localhost:8001`  
С этим: **ЛЮБОЙ** сайт может обращаться к API!

Хакер может создать `evil.com` и:
- Читать статьи
- Генерировать спам
- Отправлять комментарии

**У нас безопасно:** `CORS_ALLOW_ALL_ORIGINS = DEBUG`  
В production (DEBUG=False) будет использоваться только список `CORS_ALLOWED_ORIGINS`.

---

## 🔧 Файлы изменены

1. `requirements.txt` - добавлен `django-ratelimit==4.1.0`
2. `news/api_views.py` - rate limiting и fingerprinting
3. `news/serializers.py` - валидация изображений
4. `.env.example` - обновлен шаблон

---

## ✅ Что уже было защищено

- SQL Injection (Django ORM)
- XSS (Django templates)
- CSRF (middleware)
- JWT Authentication
- YouTube URL валидация
- HTTPS в production

---

## 📋 TODO перед production

- [ ] Смени Groq API ключ
- [ ] Смени SECRET_KEY
- [ ] Установи DEBUG=False
- [ ] Укажи свой домен в ALLOWED_HOSTS
- [ ] Укажи свой домен в CORS_ORIGINS
- [ ] Настрой PostgreSQL
- [ ] Настрой Redis
- [ ] Настрой Nginx

---

**Дата:** 2026-01-20  
**Автор:** AI Assistant + User
