'use client';

/**
 * StickyBottomAd shows a fixed advertisement at the bottom of the screen.
 * Currently disabled until actual ad units are configured.
 */
export default function StickyBottomAd() {
  // Sticky footer ad is hidden until manually enabled with real ad units
  return null;
}
